  <%= simple_form_for @return, :html => { multipart: true } do |f| %>
    <%= f.hidden_field :item_id, value: @item.id %>

    <%= f.input :return_reason %>
    <%= f.select :quantity, (1..@quantity_available.to_i) %>

    <%= f.submit 'Salvar' %>
  <% end %>